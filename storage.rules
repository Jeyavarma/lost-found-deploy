rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Chat images: publicly readable, only authenticated users can upload
    match /chat_images/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
